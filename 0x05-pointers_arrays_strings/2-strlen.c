#include "main.h"
#include <stdio.h>

/**
 * strlen - returns the length of the string.
 * @s  
 *
 * Return: the length of a string.
 */

int _strlen(char *s)
{
int len;
char *str;

len = _strlen(str);
}
