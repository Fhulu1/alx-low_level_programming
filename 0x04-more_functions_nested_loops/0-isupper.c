#include "main.h"
#include <stdio.h>

/**
 * Function checks if string is uppercase and returns 1 or 0
 * _isupper: function that checks for uppercase
 * Return: function returns 1
 */

int _isupper(int c)
{
if (_isupper(c) >= 'A' && _isupper(c) <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
